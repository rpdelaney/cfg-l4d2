echo "* Audio-visual"

cl_logofile "materials/vgui/logos/MarlonBrando.vtf"		// The sexiest man not alive

con_enable				"1"		// Enable developer console
cl_showhelp				"0"		// how 2 b a spiter
cl_showpluginmessages	"1"		// complain if I have client-side plugins loaded that prevent me from joining games
cl_forcepreload			"1"		// Preloads map for less fps drops ingame

cl_viewmodelfovsurvivor "140"

//> closed captions
closecaption			"1"		// Close captions for deaf people B-)

cc_linger_time			"0.5"	// Default: 1.0
cc_predisplay_time		"0"		// Default: 0.25  Delay before showing close caption
//cc_showblocks					// Default: cmd
//cc_showmissing				// Default: 0
cc_subtitles			"0"		// Default: 0  Don't show sound effects, just dialogue
//cc_emit						// Default: cmd  Emits a closed caption
//<
//> video / display
mat_setvideomode				"1920 1080 1"	// mat_setvideomode width height mode
mat_grain_scale_override		"0"				// Default: 1
mat_bloom_scalefactor_scalar	"0.5"			// Default: 1
mat_queue_mode					"-1"			// Default: -1
mat_queue_report				"0"				// Default: 0
mat_showmiplevels				"0"				// Default: 0
func_break_max_pieces			"0"				//
//<
//> crosshair
crosshair						"1"				// Default: 1    Because I turned it off in recording config
cl_crosshair_dynamic			"0"				// Default: 1    Stops the crosshair from showing your bullet spread and makes it remain static instead.
cl_crosshair_alpha				"255"			// Default: 255  Controls the crosshair opacity.
cl_crosshair_thickness			"2"				// Default: 2    Controls the thickness of the crosshair.
cl_colorblind					"1"				// Default: 0    Enables colorblind mode for the crosshair only. Makes it white with a black surrounding.
cl_crosshair_red				"138"			// Default: 138  Controls the amount of red color in the crosshair if colorblind mode is disabled.
cl_crosshair_green				"182"			// Default: 182  Controls the amount of green color in the crosshair if colorblind mode is disabled.
cl_crosshair_blue				"220"			// Default: 220  Controls the amount of blue color in the crosshair if colorblind mode is disabled.
//<
//> r_
r_dynamic						"1"				// Default: 1
r_cheapwaterend					"9999"			// Default: cmd
r_cheapwaterstart				"0"				// Default: cmd
r_cleardecals									// Default: cmd
r_eyemove						"0"				// Default: 1
r_eyeshift_x					"0"				// Default: 0
r_eyeshift_y					"0"				// Default: 0
r_eyeshift_z					"0"				// Default: 0
r_eyesize						"0"				// Default: 0
//<
//> netgraph
net_graphheight					"0"				// Move the graph closer to bottom
net_scale						"1"				// Smaller net_graph
net_graphpos					"1"				// Right side of the screen
//<
//> sound
// Archlinux wiki for TF2 suggests:
	// Assuming HRTF has been set up properly in the operating system, hrtf won't be enabled unless you disable the original processing. To do so, use
	//		dsp_slow_cpu 1
	// For best results, also change the following:
	//		snd_spatialize_roundrobin 1			// Doesn't exist in l4d2
	//		dsp_enhance_stereo 0
	//		snd_pitchquality 1
dsp_slow_cpu				"1"					// [0,1] - If set to 1, reduces the quality of dsp sound effects, improving performance on systems with slower CPUs.
dsp_enhance_stereo			"0"					// [0,1] - If set to 1 enhances the stereo effect, making sound richer and more atmospheric for a slight performance hit.
snd_pitchquality			"1"					//	No idea

voice_enable				"1"					// Enables voice-ingame. (Default: 1)
voice_modenable				"1"					// Enables voice-ingame. (Default: 1)
voice_scale					".7"				// Sets the voice-ingame receive volume. (Default: 0.7)

cl_customsounds				"1"					// Listen to other players' custom sounds
cl_soundfile	"hustlinerryday.wav"			// Define my own custom sound
snd_mixahead				"0.05"				// Default: 0.1  Size of the sound buffer in ms. A smaller buffer means less delay before a sound will be played, but may cause stuttering or scratching if set too low.
//<
//> hud
hud_zombieteam							"1"		// Default: 1   Show infected team status on the HUD
hud_zombieteam_showself					"0"		// Default: 0   Show your own status in the zombie team display (note: you don't get a 4th spot)
hud_takesshots							"0"		// Default: 0   Auto-save a scoreboard screenshot at the end of a map.
cl_showfps								"1"		// Default: 0
//<
//> demo
demo_recordcommands				"0"		// Default: 1  Record commands typed at console into .dem files.
cl_playback_screenshots			"0"		// Default: 0  Allows the client to playback screenshot and jpeg commands in demos.
//<

//Test_RandomChance			// Roll the dice and maybe run the command following the percentage chance.
//Test_SendKey

voice_inputfromfile				"1" // Default: 0  Get voice input from 'voice_input.wav' rather than from the microphone.
voice_loopback					"1"

// vim: ft=sourcecfg
