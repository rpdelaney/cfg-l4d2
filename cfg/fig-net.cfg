cl_interp "0";                      // No client-side arbitrary lag
cl_interp_ratio "-1";               // Always use the lowest possible interp ratio
cl_cmdrate "61";                    // Highest possible rates
cl_updaterate "61";                 // Highest possible rates
cl_lagcompensation "1";             // Highest possible rates
rate "60000";                       // Highest possible rates
cl_timeout "20";                    //[seconds] - After this many seconds without receiving a packet from the server, the client will disconnect itself.
cl_predictweapons "1";              // Who knows
cl_downloadfilter "nosounds";       //[all, none, nosounds] - Determines which files can be downloaded from a server.


// vim: ft=sourcecfg